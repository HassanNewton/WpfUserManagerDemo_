<Window
    x:Class="WpfUserManagerDemo.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Login"
    Width="300"
    Height="200">
    <StackPanel Margin="20">
        <TextBox Margin="0,0,0,10" Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />


        <PasswordBox
            x:Name="PasswordBox"
            Margin="0,0,0,10"
            PasswordChanged="PasswordBox_PasswordChanged" />

        <Button Click="Login_Click" Content="Logga in" />
        <TextBlock
            Margin="0,10,0,0"
            Foreground="Red"
            Text="{Binding Error}" />
        <Button Margin="0,20,0,0" Content="Registrera" />
        <Button Margin="0,20,0,0" Content="Registrera" />
    </StackPanel>
</Window>