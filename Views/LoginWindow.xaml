<Window x:Class="WpfUserManagerDemo.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="200" Width="300">
    <StackPanel Margin="20">
        <!-- TextBox för användarnamn -->
        <TextBox x:Name="UsernameBox" Margin="0,0,0,10"/>
        <!-- PasswordBox för lösenord -->
        <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10"/>
        <!-- Logga in-knapp -->
        <Button Content="Logga in" Click="Login_Click" Margin="0,0,0,10"/>
        <!-- Felmeddelande visas här om inloggning misslyckas -->
        <TextBlock x:Name="ErrorText" Foreground="Red" Margin="0,5,0,0"/>
    </StackPanel>
</Window>